# Spoon Rebalancing - AI-агент для автоматической ребалансировки криптопортфеля

[![Python](https://img.shields.io/badge/Python-3.12+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.121-green.svg)](https://fastapi.tiangolo.com/)
[![Next.js](https://img.shields.io/badge/Next.js-16.0-black.svg)](https://nextjs.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue.svg)](https://www.postgresql.org/)

AI-агент для автоматической ребалансировки криптопортфеля на основе рыночных условий и допустимого уровня риска. Система мониторит криптовалютные позиции по нескольким кошелькам, анализирует рыночные тренды и автоматически выполняет сделки для восстановления желаемой структуры портфеля.

## 🚀 Основные возможности

- ✅ **Мониторинг балансов** - Отслеживание балансов по нескольким кошелькам и блокчейнам
- ✅ **Анализ портфеля** - Автоматический анализ текущего распределения активов
- ✅ **Умная ребалансировка** - Расчет отклонений от целевого распределения и предложение сделок
- ✅ **Оценка комиссий** - Учет комиссий за газ и выполнение ребалансировки только при целесообразности
- ✅ **Режимы работы** - Консультационный режим (предложения) и автономный режим
- ✅ **REST API** - Полноценный REST API для интеграции
- ✅ **Веб-интерфейс** - Современный веб-интерфейс на Next.js
- ✅ **Управление стратегиями** - Создание и мониторинг стратегий ребалансировки
- ✅ **Чат с агентом** - Интерактивное общение с AI-агентом

## 🏗️ Архитектура

Проект состоит из двух основных компонентов:

### Backend (Python/FastAPI)
- **FastAPI** - REST API сервер
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - Миграции базы данных
- **SpoonOS SDK** - Интеграция с AI-агентами
- **LangGraph** - Графы для управления агентами

### Frontend (Next.js/React)
- **Next.js 16** - React фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Radix UI** - UI компоненты
- **TanStack Query** - Управление состоянием

## 📋 Требования

- Python 3.12+
- Node.js 18+
- PostgreSQL 15+
- API ключ для LLM провайдера (OpenRouter, OpenAI, Anthropic)

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd demo1
```

### 2. Установка Backend зависимостей

```bash
cd app

# Установка uv (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установка зависимостей
uv pip install -e .
```

### 3. Установка Frontend зависимостей

```bash
cd ../web
npm install
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=portfolio_rebalancer

# LLM Provider
LLM_PROVIDER=openrouter
LLM_MODEL=x-ai/grok-4.1-fast:free
OPENROUTER_API_KEY=your-api-key-here

# Optional: Bitquery API (для получения данных о балансах)
BITQUERY_CLIENT_ID=your-client-id
BITQUERY_CLIENT_SECRET=your-client-secret
```

### 5. Настройка базы данных

```bash
cd app

# Применение миграций
python init_db.py

# Или через Alembic
uv run alembic upgrade head
```

## 🚀 Запуск

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f api
```

Сервисы будут доступны:
- API: http://localhost:8000
- API Docs: http://localhost:8000/docs
- Frontend: http://localhost:3000 (если настроен)

### Вариант 2: Локальный запуск

#### Backend

```bash
cd app
python run.py
```

API сервер запустится на `http://localhost:8000`

#### Frontend

```bash
cd web
npm run dev
```

Веб-интерфейс будет доступен на `http://localhost:3000`

## 📚 API Документация

После запуска API сервера документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные endpoints

#### Кошельки
- `GET /api/wallets` - Список кошельков
- `POST /api/wallets` - Создание кошелька
- `GET /api/wallets/{id}` - Получение кошелька
- `PUT /api/wallets/{id}` - Обновление кошелька
- `DELETE /api/wallets/{id}` - Удаление кошелька
- `GET /api/wallets/{id}/balances` - Балансы кошелька

#### Стратегии
- `GET /api/strategies` - Список стратегий
- `POST /api/strategies` - Создание стратегии
- `GET /api/strategies/{id}` - Получение стратегии
- `PUT /api/strategies/{id}` - Обновление стратегии
- `DELETE /api/strategies/{id}` - Удаление стратегии
- `POST /api/strategies/{id}/execute` - Выполнение стратегии

#### Рекомендации
- `POST /api/recommendations` - Получение рекомендаций по ребалансировке

#### Чат
- `POST /api/chat` - Отправка сообщения агенту
- `GET /api/chat/history` - История чата

#### Агент
- `GET /api/agent/status` - Статус агента
- `POST /api/agent/configure` - Настройка агента

### Пример использования API

```bash
# Создание кошелька
curl -X POST "http://localhost:8000/api/wallets" \
  -H "Content-Type: application/json" \
  -d '{
    "address": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "chain": "ethereum",
    "name": "My Wallet"
  }'

# Получение рекомендаций
curl -X POST "http://localhost:8000/api/recommendations" \
  -H "Content-Type: application/json" \
  -d '{
    "wallet_ids": [1],
    "target_allocation": {
      "BTC": 40.0,
      "ETH": 35.0,
      "USDC": 25.0
    },
    "threshold_percent": 5.0
  }'
```

## 🎯 Использование

### Создание стратегии ребалансировки

1. Создайте кошельки через API или веб-интерфейс
2. Создайте стратегию с целевым распределением:
   ```json
   {
     "name": "Conservative Portfolio",
     "target_allocation": {
       "BTC": 40.0,
       "ETH": 35.0,
       "USDC": 25.0
     },
     "threshold_percent": 5.0,
     "wallet_ids": [1, 2]
   }
   ```
3. Система автоматически мониторит стратегию и генерирует рекомендации

### Режимы работы

#### Консультационный режим (consultation)
Агент анализирует портфель и предлагает конкретные сделки, но не выполняет их автоматически. Пользователь может просмотреть рекомендации и принять решение.

#### Автономный режим (autonomous)
Агент автоматически выполняет сделки для ребалансировки портфеля. **Внимание:** В текущей версии это демо-режим, реальные транзакции не выполняются.

## 📁 Структура проекта

```
demo1/
├── app/                          # Backend приложение
│   ├── app/
│   │   ├── agents/              # AI агенты
│   │   ├── api/                 # API слой
│   │   │   ├── routes/          # API роуты
│   │   │   └── main.py          # FastAPI приложение
│   │   ├── core/                # Основные настройки
│   │   ├── db/                  # Работа с БД
│   │   │   ├── models.py        # SQLAlchemy модели
│   │   │   └── session.py       # Сессии БД
│   │   ├── graphs/              # LangGraph графы
│   │   ├── services/            # Бизнес-логика
│   │   ├── tools/               # Инструменты агентов
│   │   └── utils/               # Утилиты
│   ├── alembic/                 # Миграции БД
│   ├── run.py                   # Точка входа
│   └── pyproject.toml           # Зависимости Python
│
├── web/                          # Frontend приложение
│   ├── src/
│   │   ├── app/                 # Next.js страницы
│   │   ├── components/          # React компоненты
│   │   ├── lib/                 # Утилиты
│   │   └── providers/           # React провайдеры
│   └── package.json             # Зависимости Node.js
│
├── docker-compose.yml            # Docker конфигурация
└── README.md                     # Этот файл
```

## 🔧 Разработка

### Запуск тестов

```bash
# Backend тесты
cd app
pytest

# Frontend тесты
cd web
npm test
npm run test:e2e
```

### Создание миграций

```bash
cd app
uv run alembic revision --autogenerate -m "description"
uv run alembic upgrade head
```

### Линтинг

```bash
# Backend
cd app
ruff check .

# Frontend
cd web
npm run lint
```

## 🔒 Безопасность

⚠️ **Важно:**
- В production версии необходимо добавить аутентификацию для API
- Приватные ключи кошельков должны храниться в безопасном хранилище
- Автономный режим должен быть тщательно протестирован перед использованием с реальными средствами
- Всегда проверяйте рекомендации агента перед выполнением транзакций
- Настройте CORS для конкретных доменов в production

## 🌐 Поддерживаемые блокчейны

- Ethereum
- Arbitrum
- Polygon
- (добавление новых блокчейнов в разработке)

## 📖 Дополнительная документация

- [Архитектура проекта](app/ARCHITECTURE.md)
- [Быстрый старт](app/QUICKSTART.md)
- [Полная документация ребалансировщика](app/PORTFOLIO_REBALANCER_README.md)
- [Настройка Docker](app/DOCKER_SETUP.md)
- [Миграции БД](app/MIGRATIONS.md)

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект является демонстрационным примером использования SpoonOS.

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте документацию в папке `app/`
2. Откройте issue в репозитории
3. Проверьте логи приложения в `app/logs/`

## 🎉 Благодарности

- [SpoonOS](https://spoonos.ai) - Платформа для создания AI-агентов
- [FastAPI](https://fastapi.tiangolo.com) - Современный веб-фреймворк
- [Next.js](https://nextjs.org) - React фреймворк
- [LangGraph](https://langchain-ai.github.io/langgraph/) - Управление агентами

---

**Версия:** 2.0.0  
**Последнее обновление:** 2025
